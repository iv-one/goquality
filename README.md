# quality-history

Published by CI on every push to `main` (see `.github/workflows/go.yml`):

- `badges/score.svg`, `badges/grade.svg`: README badges
- `report.txt`: the full `goquality -v` report the badges link to

Not for development; do not merge into `main`.
